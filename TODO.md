# TODO

- Add `python-build-deb.yml` shared workflow and migrate `wg-netns` from its custom `jtdor/build-deb-action` workflow.
- Extract `sinad_meter` instrument driver symlinks (`hp_8662a.py`, `keithley_2015.py`, `rs_smb100a.py`) into a shared repo and wire up as git submodules in both `sinad_meter` and `ee_stuff`.
