[project]
name = "python-uv-fixture"
version = "0.1.0"
description = "Fixture: uv path with the tools locked as project dependencies"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest", "ruff"]

[tool.uv]
package = false
